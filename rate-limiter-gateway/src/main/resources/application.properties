spring.application.name=rate-limiter-gateway

server.port=${PORT:8000}

# Service URLs
rate-limiter.url=${RATE_LIMITER_URL}/ratelimit/check
url-shortener.url=${URL_SHORTENER_URL}/urlShortener
registration.url=${REGISTRATION_URL}/register